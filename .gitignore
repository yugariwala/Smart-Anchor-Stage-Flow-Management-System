node_modules/
dist/
.wrangler/
.firebase/
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example
coverage/
test-results/
artifacts/
playwright-report/
*.local
.DS_Store

# Credentials handed over for local setup — never committed
firebase-config.txt

# Scratch output from fixture tooling
.fixture-source.tmp

# UX walkthrough working captures. The two sets cited by docs/ux-audit.md are committed;
# these two were intermediate and are reproducible by re-running the walkthrough.
docs/ux-screens-polish/
docs/ux-screens-next/
